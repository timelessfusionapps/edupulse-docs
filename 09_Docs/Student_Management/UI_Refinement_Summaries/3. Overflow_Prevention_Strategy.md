## Purpose
Eradicate RenderFlex overflow errors entirely.

## Implementation
- Avoid `SingleChildScrollView(horizontal)` as a crutch.
- Use `Expanded` and `Flexible` heavily in custom list rows.
- Implement `TextOverflow.ellipsis` globally on dynamic text.
- Set strict minimum widths via `BoxConstraints` for desktop containers before breaking down to the tablet layout.