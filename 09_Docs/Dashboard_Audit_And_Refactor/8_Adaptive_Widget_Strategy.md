# Adaptive Widget Strategy

Introduce `adaptive_dashboard_header.dart` to scale greeting typography smoothly, and `adaptive_kpi_layout.dart` to handle the transition from Desktop Grid -> Mobile Carousel.