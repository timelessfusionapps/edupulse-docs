# Tablet Dashboard UX Strategy

1. Maintain a 2-column layout (unlike mobile), but shift to a 50/50 split instead of 60/40.
2. Reduce chart complexity dynamically to prevent label overlap.
3. Keep typography readable without scaling it down linearly.