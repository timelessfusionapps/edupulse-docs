# Notification Data Architecture Report
Notifications include severity, acknowledging status, and read flags. Data is queried deterministically by `createdAt` and `documentId`. The repository provides future-proofing for moderation integration.
