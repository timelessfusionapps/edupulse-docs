# Dashboard Analytics Snapshot Versioning Report
Entities (`DashboardAnalyticsEntity`) now require `snapshotVersion`, `generatedAt`, `aggregationType`, and `rangeType`. This guarantees backward-compatible evolution for long-term analytics structural modifications.
